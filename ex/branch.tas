_start:
    b <- 10             // set up loop constraint
top:
    b <- b - 1          // decrement loop variable
    c <- b > a          // compare b to 0 (`a' is always 0)
    p <- [p + c + 2]    // write PC from a computed location:
    .word @top          //     target if condition true
    .word @done         //     target if condition false
done:
    illegal
