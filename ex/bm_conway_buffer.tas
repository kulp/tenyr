.set VGA_ROWS, 32
.set VGA_COLS, 64

# Due to intentional limitations on @-expressions, we cannot actually compute
# some of the expressions shown below.

.global databuf
# databuf: .zero (@VGA_ROWS * @VGA_COLS)
databuf: .zero (32 * 64)
