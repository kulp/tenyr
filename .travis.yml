language: c
compiler:
  - gcc
  - clang
os:
  - linux
  - osx
script: "make V=1 SDL=0 && make check && make local-install"
notifications:
    irc:
        channels:
          - "irc.freenode.org#tenyr"
        use_notice: true
        skip_join: true
